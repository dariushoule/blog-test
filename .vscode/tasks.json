{
    "version": "2.0.0",
    "tasks": [
        {
            "label": "install dependencies",
            "type": "shell",
            "command": "pip install requests && python3 .vscode/ensure-deps.py",
            "runOptions": { "runOn": "folderOpen" }
        }
    ]
}